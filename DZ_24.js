/*
const inputElem = document.getElementsByTagName('input')

let divTag;
    inputElem[0].addEventListener('focus', () => {
    divTag = document.createElement('div')
    document.body.appendChild(divTag)
    const text = document.createTextNode('при фокусировки появляется сбоку div')
    divTag.appendChild(text)
    divTag.setAttribute('style', 'display: inline-block')
});
    inputElem[0].addEventListener('blur', () => {
    divTag.remove();
})
*/
